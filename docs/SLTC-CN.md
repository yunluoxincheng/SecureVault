> 中文版说明：本文件为机器翻译生成的中文版本，保留原始需求编号、测试ID与文档结构，建议与英文原文对照使用。

# SecureVault — 系统级测试用例 (SLTC)

> 基于 SRS v1.1 |日期：2026-03-25
> 作者：系统测试主管 (SLTC)

**文档范围（2026-03-25）：** 测试用例编号不变。**测试执行与退出准则**以 **Android** 与 **Windows Desktop** 为**必做环境**。**iOS** 存根类用例与 **macOS/Linux 桌面** 打包验证为**可选/暂缓**，除非 [PLATFORM-SCOPE.md](PLATFORM-SCOPE.md) 另有更新。

---

## 覆盖范围摘要

| 方面 | 数数 |
|-----------|-------|
| 测试用例总数 | 165 |
| 涵盖的模块 | 11 / 11 |
| 涵盖的系统状态 | 4 / 4（未设置、锁定、解锁、解锁+安全模式） |
| 涵盖的核心流程 | 5 / 5（设置、添加、检索、删除、更改密码） |
| 正常场景 | 58 |
| 错误场景 | 47 |
| 边缘情况 | 28 |
| 安全场景 | 32 |

---

## 1. 模块：密钥派生（Argon2id KDF）

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| KDF-001 | 氩气2id KDF | 解锁 | 正常：使用默认配置导出密钥 | libsodium 已初始化；有效密码 CharArray； 16字节盐；配置 = (128 MB, 3 iter, 4 par, 32 out) | 1. 调用 `deriveKey(密码, salt, config)` | 返回 32 字节派生密钥。密码 CharArray 被擦除（全为零）。 | 批判的 |
| KDF-002 | 氩气2id KDF | 解锁 | 正常：相同的输入产生相同的输出 | 与 KDF-001 相同的密码、盐、配置 | 1. 使用相同的输入调用“deriveKey”两次 | 两个调用都返回相同的 32 字节数组 | 批判的 |
| KDF-003 | 氩气2id KDF | 解锁 | 正常：不同的密码产生不同的密钥 | 两个不同的密码；相同的盐和配置 | 1. 使用密码 A 导出密钥 2. 使用密码 B 导出密钥 | 返回的键不同 | 批判的 |
| KDF-004 | 氩气2id KDF | 解锁 | 正常：不同的盐产生不同的密钥 | 密码相同；两个不同的 16 字节盐；相同的配置 | 1. 使用盐 A 导出密钥 2. 使用盐 B 导出密钥 | 返回的键不同 | 高的 |
| KDF-005 | 氩气2id KDF | 解锁 | 错误：libsodium 未初始化 | libsodium 未初始化 | 1. 调用 `deriveKey(密码, salt, config)` | 抛出异常。密码 CharArray 仍然在“finally”块中被擦除。 | 高的 |
| KDF-006 | 氩气2id KDF | 解锁 | 安全性：成功推导后密码 CharArray 被擦除 | 有效输入 | 1. 创建 CharArray 密码 2. 调用 `deriveKey` 3. 调用后检查密码数组 | 密码 CharArray 的每个元素 == `\u0000` | 批判的 |
| KDF-007 | 氩气2id KDF | 解锁 | 安全性：密码 CharArray 在派生失败后被擦除 | 无效配置导致异常 | 1. 使用错误的配置调用 `deriveKey` 2. 异常后检查密码数组 | 密码 CharArray 的每个元素 == `\u0000` | 批判的 |
| KDF-008 | 氩气2id KDF | 解锁 | Edge：单字符密码 | 密码 = CharArray(['a']);有效的盐+配置 | 1. 调用`deriveKey` | 返回 32 字节密钥，无错误 | 低的 |
| KDF-009 | 氩气2id KDF | 解锁 | Edge：Unicode 密码（CJK 字符） | 密码=汉字CharArray；有效的盐+配置 | 1. 调用`deriveKey` | 返回 32 字节密钥，无错误 | 低的 |
| KDF-010 | 氩气2id KDF | 解锁 | Edge：最低 Argon2 配置 | 配置 = (8192 KB, 1 iter, 1 par, 32 out) | 1. 调用`deriveKey` | 返回 32 字节密钥。派生完成速度比默认配置更快。 | 中等的 |

## 2. 模块：认证加密（XChaCha20-Poly1305）

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| ENC-001 | XChaCha20-Poly1305 | 解锁 | 正常：加密然后解密往返 | libsodium 已初始化；有效的 32 字节密钥；明文=“你好”字节 | 1. 使用密钥加密明文 2. 使用相同密钥解密结果 | 解密输出==原始明文 | 批判的 |
| ENC-002 | XChaCha20-Poly1305 | 解锁 | 正常：每次加密都会产生唯一的密文 | 相同的明文和密钥 | 1. 使用相同的密钥对相同的明文进行两次加密 | 两个 EncryptedData 对象具有不同的“iv”字段和不同的“ciphertext”字段（由于随机数） | 批判的 |
| ENC-003 | XChaCha20-Poly1305 | 解锁 | 正常：EncryptedData结构有效 | 有效明文和密钥 | 1. 加密明文 2. 检查返回的 EncryptedData | `version == "v2"`、`iv.size == 24`、`tag.size == 16`、`ciphertext.isNotEmpty()` | 高的 |
| ENC-004 | XChaCha20-Poly1305 | 解锁 | Normal：存储格式序列化往返 | 有效的 EncryptedData 对象 | 1. 调用 `toStorageFormat()` 2. 对结果调用 `fromStorageFormat()` | 重建的 EncryptedData 具有相同的 `version`、`iv`、`ciphertext`、`tag` | 高的 |
| ENC-005 | XChaCha20-Poly1305 | 解锁 | 正常： 加密到存储格式 + 解密从存储格式 | 有效明文和密钥 | 1. `encryptToStorageFormat(plaintext, key)` 2. `decryptFromStorageFormat(结果, key)` | 解密后的字节数 == 原始明文字节数 | 高的 |
| ENC-006 | XChaCha20-Poly1305 | 解锁 | 错误：密钥大小 < 32 字节 | 键 = 16 字节数组 | 1. 调用“加密（明文，短密钥）” | 抛出“IllegalArgumentException”，消息包含“密钥必须为 32 字节” | 批判的 |
| ENC-007 | XChaCha20-Poly1305 | 解锁 | 错误：密钥大小 > 32 字节 | 键 = 64 字节数组 | 1. 调用 `encrypt(plaintext, longKey)` | 抛出“IllegalArgumentException” | 高的 |
| ENC-008 | XChaCha20-Poly1305 | 解锁 | 错误：空明文 | 明文 = 空 ByteArray；有效密钥 | 1. 调用“加密(emptyArray, key)” | `IllegalArgumentException` 消息为“纯文本不能为空” | 高的 |
| ENC-009 | XChaCha20-Poly1305 | 解锁 | 安全性：使用错误的密钥解密 | 使用密钥 A 加密 | 1. 使用密钥 A 加密 2. 使用密钥 B 解密（不同的 32 字节密钥） | libsodium 抛出身份验证失败异常。没有返回明文。 | 批判的 |
| ENC-010 | XChaCha20-Poly1305 | 解锁 | 安全性：检测到密文被篡改 | 有效的加密数据 | 1. 加密明文 2. 翻转“ciphertext”数组中的一位 3. 尝试解密 | 认证失败。抛出异常。没有返回明文。 | 批判的 |
| ENC-011 | XChaCha20-Poly1305 | 解锁 | 安全性：检测到标签被篡改 | 有效的加密数据 | 1. 加密明文 2. 翻转 `tag` 数组中的一位 3. 尝试解密 | 认证失败。抛出异常。 | 批判的 |
| ENC-012 | XChaCha20-Poly1305 | 解锁 | 安全性：检测到 IV 被篡改 | 有效的加密数据 | 1. 加密明文 2. 修改 `iv` 中的一个字节 3. 尝试解密 | 认证失败。抛出异常。 | 批判的 |
| ENC-013 | XChaCha20-Poly1305 | 解锁 | 错误：版本字符串不受支持 | 版本为“v1”的加密数据 | 1. 构造 EncryptedData(version="v1", ...) 2. 调用 `decrypt` | `IllegalArgumentException` 带有消息“不支持的版本：v1” | 中等的 |
| ENC-014 | XChaCha20-Poly1305 | 解锁 | 错误：随机数大小无效 | iv.size = 12（不是 24）的 EncryptedData | 1. 构造 EncryptedData(iv=12 bytes) 2. 调用 `decrypt` | “IllegalArgumentException”，包含有关无效随机数大小的消息 | 中等的 |
| ENC-015 | XChaCha20-Poly1305 | 解锁 | 错误：标签大小无效 | EncryptedData 的 tag.size = 8（不是 16） | 1. 构造 EncryptedData(tag=8 bytes) 2. 调用 `decrypt` | “IllegalArgumentException”，包含有关无效标签大小的消息 | 中等的 |
| ENC-016 | XChaCha20-Poly1305 | 解锁 | 边缘：大明文 (1 MB) | 1 MB 字节数组；有效密钥 | 1. 加密 2. 解密 | 往返成功。解密==原始。 | 低的 |
| ENC-017 | XChaCha20-Poly1305 | 解锁 | 错误：JSON 存储格式格式错误 | JSON 字符串无效 | 1. 调用 `decryptFromStorageFormat("{invalid", key)` | JSON 解析抛出异常 | 中等的 |
| ENC-018 | XChaCha20-Poly1305 | 解锁 | 错误：存储格式中的 base64 无效 | JSON 的 iv 字段的 base64 无效 | 1. 使用错误的 base64 调用 `decryptFromStorageFormat` | Base64 解码抛出异常 | 中等的 |

## 3. 模块：会话管理器

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| SES-001 | 会话管理器 | 锁定 | 正常：解锁加载DataKey | SessionManager处于锁定状态；有效的 32 字节 DataKey | 1. 调用 `unlock(dataKey)` 2. 检查 `isUnlocked()` 3. 检查 `sessionState` | `isUnlocked() == true`。 `sessionState == 已解锁`。 `getDataKey()` 返回与原始数据匹配的数据。 | 批判的 |
| SES-002 | 会话管理器 | 解锁 | 正常：锁擦除 DataKey | 使用已知的 DataKey 解锁会话 | 1. 调用 `lock()` 2. 检查 `isUnlocked()` 3. 检查 `sessionState` | `isUnlocked() == false`。 `sessionState == 已锁定`。 | 批判的 |
| SES-003 | 会话管理器 | 解锁 | 正常：getDataKey 返回副本 | 会话已解锁 | 1. 调用两次`getDataKey()` | 两个返回的数组具有相同的内容，但是不同的对象引用 | 高的 |
| SES-004 | 会话管理器 | 锁定 | 错误：锁定时获取DataKey | 会话已锁定 | 1. 调用`getDataKey()` | 抛出 `IllegalStateException("会话已锁定")` | 批判的 |
| SES-005 | 会话管理器 | 解锁 | 正常：getDataKey刷新lastActivityTime | 会话已解锁；记下当前时间 | 1. 等待 100 毫秒 2. 调用 `getDataKey()` 3. 调用 `checkAutoLock()`，超时时间 > 100 毫秒 | `checkAutoLock()` 返回 false（由 `getDataKey` 扩展的会话） | 高的 |
| SES-006 | 会话管理器 | 解锁 | 正常：onAppBackground 立即锁定 | 会话已解锁； `lockTimeoutMs = -1` | 1. 调用`onAppBackground()` | 返回真。 `isUnlocked() == false`。 `sessionState == 已锁定`。 | 高的 |
| SES-007 | 会话管理器 | 解锁 | 正常：onAppBackground 非立即超时 | 会话已解锁； `lockTimeoutMs = 300000` | 1. 调用`onAppBackground()` | 返回假。 `isUnlocked() == true`。记录了“backgroundEnteredAtMs”。 | 高的 |
| SES-008 | 会话管理器 | 解锁 | 正常：onAppForeground 超时内 | 背景记录；已过去 < 超时 | 1. 调用 `onAppBackground()` 2. 等待 < 超时 3. 调用 `onAppForeground()` | 返回假。会话保持解锁状态。 `lastActivityTime` 已刷新。 | 高的 |
| SES-009 | 会话管理器 | 解锁 | 正常：onAppForeground超过超时 | 背景记录； `lockTimeoutMs = 1000` | 1. 调用 `onAppBackground()` 2. 等待 >= 1000ms 3. 调用 `onAppForeground()` | 返回真。会话已锁定。数据键已擦除。 | 高的 |
| SES-010 | 会话管理器 | 解锁 | 正常：超时内检查AutoLock | 会话已解锁； `lockTimeoutMs = 300000`;最近的活动 | 1. 调用 `checkAutoLock()` | 返回假。会话保持解锁状态。 | 中等的 |
| SES-011 | 会话管理器 | 解锁 | 正常：checkAutoLock超过超时 | 会话已解锁； `lockTimeoutMs = 100`;上次活动 > 100 毫秒前 | 1. 等待 > 100ms，而不调用 getDataKey 2. 调用 `checkAutoLock()` | 返回真。会话已锁定。 | 中等的 |
| SES-012 | 会话管理器 | 解锁 | 边沿：超时 = 0（从不自动锁定） | `lockTimeoutMs = 0` | 1. 调用 `onAppBackground()` 2. 等待任意时间 3. 调用 `onAppForeground()` 4. 调用 `checkAutoLock()` | 全部返回 false。会话永远不会自动锁定。 | 中等的 |
| SES-013 | 会话管理器 | 解锁 | Edge：重新解锁替换之前的钥匙 | 用钥匙A解锁；然后用keyB解锁 | 1. `解锁（keyA）` 2. `解锁（keyB）` 3. `getDataKey()` | 返回的 key 与 keyB 匹配。 keyA 的 SensitiveData 包装器已关闭（擦除）。 | 批判的 |
| SES-014 | 会话管理器 | 锁定 | 正常：已锁定时的 onAppBackground | 会话已锁定 | 1. 调用`onAppBackground()` | 返回假。没有状态改变。 | 低的 |
| SES-015 | 会话管理器 | 解锁 | 安全性：锁上的 DataKey 被擦除 | 锁定前捕获 DataKey 引用 | 1. `unlock(key)` 2. 获取内部引用 3. `lock()` 4. 检查底层字节数组 | 字节数组全为零（由 MemorySanitizer 擦除） | 批判的 |

## 4. 模块：平台密钥库

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| PKS-001 | 平台密钥库 | 解锁 | 正常：存储和检索DataKey | 空 PlatformKeyStore | 1. `storeDeviceKey(key)` 2. `getDeviceKey()` | 检索到的密钥与原始密钥匹配 | 批判的 |
| PKS-002 | 平台密钥库 | 任何 | 正常：存储后有DeviceKey | 已存储密钥 | 1. `storeDeviceKey(key)` 2. `hasDeviceKey()` | 返回真 | 高的 |
| PKS-003 | 平台密钥库 | 任何 | 正常：存储之前有DeviceKey | 空荡荡的商店 | 1.`hasDeviceKey()` | 返回错误 | 高的 |
| PKS-004 | 平台密钥库 | 任何 | 正常：删除DeviceKey | 已存储密钥 | 1. `deleteDeviceKey()` 2. `hasDeviceKey()` 3. `getDeviceKey()` | `hasDeviceKey() == false`。 `getDeviceKey() == null`。 | 高的 |
| PKS-005 | 平台密钥库 | 任何 | 正常：覆盖现有密钥 | 密钥 A 已存储 | 1. `storeDeviceKey(keyA)` 2. `storeDeviceKey(keyB)` 3. `getDeviceKey()` | 检索到的密钥与 keyB 匹配 | 高的 |
| PKS-006 | 平台密钥库 | 任何 | 错误：未存储时获取设备密钥 | 空荡荡的商店 | 1.`getDeviceKey()` | 返回空值 | 中等的 |
| PKS-007 | 平台密钥库 | 任何 | Edge (Android)：isHardwareBacked | 带有 TEE 的 Android 设备 | 1. `isHardwareBacked()` | 在具有 TEE/StrongBox 的设备上返回 true，否则返回 false | 中等的 |
| PKS-008 | 平台密钥库 | 任何 | 安全性（桌面）：XOR 密钥可恢复 | 桌面平台；密钥已存储 | 1. 直接读取 Java Preferences 2. 提取 XOR 主密钥和加密 blob 3. 对它们进行 XOR | 恢复的字节与原始 DataKey 匹配（显示弱点） | 批判的 |

## 5. 模块：安全模式管理器

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| SMM-001 | 安全模式管理器 | 解锁 | 正常：使用 securityMode=false 加密密码 | 有效的数据密钥； `安全模式 = false` | 1. 调用 `encryptPasswordForStorage("secret", dataKey, false)` | 返回加密的 JSON 字符串。可以用DataKey解密。 | 高的 |
| SMM-002 | 安全模式管理器 | 解锁 | 正常：使用 securityMode=true 加密密码 | 有效的数据密钥； `安全模式 = true`;尚不存在 SecureModeKey | 1. 调用 `encryptPasswordForStorage("secret", dataKey, true)` | SecureModeKey 生成并加密存储在Vault_config 中。使用 SecureModeKey 加密的密码。 | 批判的 |
| SMM-003 | 安全模式管理器 | 解锁 | 正常：使用 securityMode=true 解密密码 | 使用 securityMode=true 加密的条目 | 1. 使用 securityMode=true 进行加密 2. 调用 `decryptPasswordForRead(encrypted, dataKey, true)` | 返回原始明文“秘密” | 批判的 |
| SMM-004 | 安全模式管理器 | 解锁 | 安全性：SecureModeKey 加密后擦除 | 有效输入；安全模式=true | 1. 使用 securityMode=true 调用 `encryptPasswordForStorage` 2. 调用后检查 secureModeKey | secureModeKey 被擦除（全零）——通过“finally”块执行进行验证 | 批判的 |
| SMM-005 | 安全模式管理器 | 解锁 | 安全性：SecureModeKey 解密后被擦除 | 有效输入；安全模式=true | 1. 使用 securityMode=true 调用 `decryptPasswordForRead` 2. 检查 secureModeKey | secureModeKey 被擦除（全为零） | 批判的 |
| SMM-006 | 安全模式管理器 | 解锁 | 正常：usePassword 复制到剪贴板并擦除 | 有效的加密条目；模拟安全剪贴板 | 1. 调用 `usePassword(encrypted, dataKey, true)` | 使用正确的明文调用“SecureClipboard.copy()”。调用了“scheduleAutoClear()”。解密字节被擦除（全为零）。 | 批判的 |
| SMM-007 | 安全模式管理器 | 解锁 | 安全性：usePassword 在剪贴板失败时擦除字节 | SecureClipboard.copy() 抛出 | 1. 使用模拟剪贴板调用`usePassword`，抛出异常 | 解密的字节仍然在“finally”块中被擦除 | 批判的 |
| SMM-008 | 安全模式管理器 | 解锁 | 正常：SecureModeKey 在调用之间重复使用 | SecureModeKey 已存在于Vault_config 中 | 1. 使用 securityMode=true 进行加密 2. 使用 securityMode=true 加密另一个密码 | 两者都使用相同的 SecureModeKey（从配置中检索，而不是重新生成）。两者都可以解密。 | 高的 |
| SMM-009 | 安全模式管理器 | 解锁 | 错误：使用错误的 DataKey 解密 securityMode 密码 | 使用 dataKeyA 加密的密码 | 1. 尝试 `decryptPasswordForRead(encrypted, dataKeyB, true)` | 抛出异常（无法使用错误的 DataKey 解密 SecureModeKey） | 批判的 |
| SMM-010 | 安全模式管理器 | 解锁 | 正常： isEnabled / setEnabled 切换 | 可访问配置存储库 | 1. `isEnabled()` → false 2. `setEnabled(true)` 3. `isEnabled()` → true 4. `setEnabled(false)` 5. `isEnabled()` → false | 每次调用都会返回持久值 | 中等的 |
| SMM-011 | 安全模式管理器 | 解锁 | Edge：全局切换关闭后 securityMode=true 条目仍使用 SecureModeKey | 使用 securityMode=true 创建的条目；然后禁用全局切换 | 1. 使用 securityMode=true 创建条目 2. 禁用全局 SecurityMode 3. 使用 securityMode=true 标志解密条目 | 解密使用 SecureModeKey（每个条目标志控制加密密钥选择，而不是全局切换） | 高的 |

## 6. 模块：安全剪贴板

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| 夹子-001 | 安全剪贴板 | 解锁 | 正常：将文本复制到剪贴板 | 平台剪贴板可用 | 1. 调用 `copy("myPassword", "密码")` | 系统剪贴板包含“myPassword” | 高的 |
| 夹子-002 | 安全剪贴板 | 解锁 | 正常：30秒后自动清除 | 文本已复制到剪贴板 | 1. 调用 `copy("secret", "Password")` 2. 调用 `scheduleAutoClear()` 3. 等待 30 秒以上 | 剪贴板内容替换为空字符串 | 批判的 |
| 剪辑-003 | 安全剪贴板 | 解锁 | 正常：立即清除 | 剪贴板上的文本 | 1. 调用 `copy("secret", "Password")` 2. 调用 `clear()` | 剪贴板内容立即替换为空字符串 | 高的 |
| 夹子-004 | 安全剪贴板 | 解锁 | 正常：重新复制重置计时器 | 第一份副本完成 | 1. `copy("passwordA")` + `scheduleAutoClear()` 2. 等待 15 秒 3. `copy("passwordB")` + `scheduleAutoClear()` 4. 等待 20 秒 | 剪贴板包含“passwordB”（尚未清除 - 自上次复制以来仅过去了 30 秒中的 20 秒） | 批判的 |
| 夹子-005 | 安全剪贴板 | 解锁 | 正常：第二个副本满 30 秒后重新复制会清除 | 接CLIP-004 | 5. 再等待 10 秒（从第二个副本开始总共 30 秒） | 剪贴板为空 | 批判的 |
| 夹子-006 | 安全剪贴板 | 解锁 | 安全性：剪贴板在 30 秒内未清除 | 复制文本 | 1. `copy("secret")` + `scheduleAutoClear()` 2. 在第 29 秒读取剪贴板 | 剪贴板仍然包含“秘密” | 高的 |
| 剪辑-007 | 安全剪贴板 | 解锁 | Edge (iOS)：复制是无操作的 | iOS平台 | 1. 调用 `copy("test")` 2. 读取 iOS 剪贴板 | iOS 剪贴板未更改（方法是存根无操作） | 中等的 |
| 剪辑-008 | 安全剪贴板 | 解锁 | Edge (iOS)：scheduleAutoClear 是无操作的 | iOS平台 | 1. 调用 `scheduleAutoClear()` | 没有崩溃。没有安排计时器。 | 中等的 |
| 剪辑-009 | 安全剪贴板 | 解锁 | 边缘：多次快速复制 | — | 1. `copy("a")` + `scheduleAutoClear()` 2. 立即 `copy("b")` + `scheduleAutoClear()` 3. 立即 `copy("c")` + `scheduleAutoClear()` | 剪贴板包含“c”。只有一个计时器处于活动状态。计时器在“c”复制后 30 秒触发。 | 高的 |

## 7. 模块：内存消毒器

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| MEM-001 | 内存消毒剂 | 任何 | 正常：擦除 ByteArray | 具有非零值的 32 字节 ByteArray | 1. 调用 `wipe(data)` 2. 调用 `isWiped(data)` | `isWiped()` 返回 true。每个字节 == 0。 | 批判的 |
| MEM-002 | 内存消毒剂 | 任何 | 正常：擦除 CharArray | 带字符的 CharArray | 1. 调用 `wipe(data)` 2. 调用 `isWiped(data)` | `isWiped()` 返回 true。每个字符 == `\u0000`。 | 批判的 |
| MEM-003 | 内存消毒剂 | 任何 | 正常：擦除 IntArray | 具有非零值的 IntArray | 1. 调用 `wipe(data)` | 每个整数 == 0 | 高的 |
| MEM-004 | 内存消毒剂 | 任何 | 正常：多遍覆盖 | 字节数组；通过= 3 | 1. 在擦除期间，验证各遍之间的中间状态 | 通过 0：所有字节 = 0x00。第 1 遍：所有字节 = 0x01。第 2 遍：所有字节 = 0x02。最终：所有字节 = 0x00。 | 高的 |
| MEM-005 | 内存消毒剂 | 任何 | Edge：擦除空数组 | 大小为 0 的 ByteArray | 1. 调用 `wipe(emptyByteArray)` | 没有崩溃。 `isWiped()` 返回 true。 | 低的 |
| MEM-006 | 内存消毒剂 | 任何 | 边缘：用遍数擦拭 = 1 | 字节数组；通过 = 1 | 1. 调用 `wipe(data, Passs = 1)` | 所有字节都被覆盖一次然后归零。 `isWiped()` 返回 true。 | 低的 |
| MEM-007 | 内存消毒剂 | 任何 | 错误：用遍数擦除 = 0 | 字节数组；通过 = 0 | 1. 调用 `wipe(data, Passs = 0)` | `IllegalArgumentException("通过必须是正数")` | 低的 |
| MEM-008 | 内存消毒剂 | 任何 | 正常：SensitiveData.ofByteArray 存储副本 | 32字节数组 | 1. 创建 `SensitiveData.ofByteArray(original)` 2. 修改 `original[0]` 3. 调用 `get()` | 返回数据！=修改原数据（副本独立） | 高的 |
| MEM-009 | 内存消毒剂 | 任何 | 正常：SensitiveData.close 擦除数据 | 创建敏感数据 | 1. `SensitiveData.ofByteArray(key)` 2. `close()` 3. `isAvailable` | `isAvailable == false`。底层数组已擦除。 | 批判的 |
| MEM-010 | 内存消毒剂 | 任何 | 错误：关闭后 SensitiveData.get | 敏感数据已关闭 | 1. 创建并关闭 SensitiveData 2. 调用 `get()` | `IllegalStateException("SensitiveData 已关闭")` | 高的 |
| MEM-011 | 内存消毒剂 | 任何 | 边缘：SensitiveData.close 被调用两次 | 创建敏感数据 | 1. `close()` 2. `close()` | 没有崩溃。第二次关闭是幂等的。 | 低的 |

## 8. 模块：生物识别认证

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| 生物-​​001 | 生物识别认证 | 锁定 | 正常：生物识别解锁成功 | 安卓;已登记生物识别； PlatformKeyStore 中的 DeviceKey | 1. 调用 `authenticate("Title", "Subtitle")` 2. 用户成功扫描指纹 | 返回 `BiometricResult.Success` | 高的 |
| 生物-​​002 | 生物识别认证 | 锁定 | 错误：生物识别身份验证失败 | 安卓;指纹错误 | 1. 调用 `authenticate` 2. 用户提供了错误的指纹 | 返回 `BiometricResult.Failed` | 高的 |
| BIO-003 | 生物识别认证 | 锁定 | 正常：用户取消生物识别提示 | 安卓 | 1. 调用 `authenticate` 2. 用户点击取消 | 返回“BiometricResult.Cancelled” | 中等的 |
| BIO-004 | 生物识别认证 | 任何 | 正常：生物识别在桌面上不可用 | 桌面平台 | 1. 调用`isAvailable()` | 返回错误 | 中等的 |
| 生物-​​005 | 生物识别认证 | 任何 | 正常：身份验证在桌面上返回 NotAvailable | 桌面平台 | 1. 调用 `authenticate("Title", "Sub")` | 返回“BiometricResult.NotAvailable” | 中等的 |
| 生物-​​006 | 生物识别认证 | 锁定 | Edge：BiometricState 速率限制器（未有线） | 生物识别状态实例 | 1. 调用 `recordFailure()` 5 次 2. 调用 `isLockedOut()` 3. 调用 `canAuthenticate()` | `isLockedOut() == true`。 `canAuthenticate() == false`。 `getRemainingAttempts() == 0`。注意：此类未连接到实际的身份验证流程中。 | 高的 |
| BIO-007 | 生物识别认证 | 锁定 | Edge：BiometricState 锁定到期 | 生物识别状态被锁定 | 1. 锁定 BiometricState 2. 等待 30 秒 3. 调用 `isLockedOut()` | 返回假。计数器重置。 | 高的 |
| 生物-​​008 | 生物识别认证 | 锁定 | 正常：生物识别状态去抖 | 具有去抖功能的生物识别状态 = 500ms | 1. 记录成功 2. 立即调用`canAuthenticate()` | 返回 false（在去抖窗口内） | 中等的 |
| BIO-009 | 生物识别认证 | 锁定 | 安全性：重复的生物识别失败（无应用程序级锁定） | 安卓; 10+错误指纹 | 1. 调用 `authenticate()` 10 次，每次返回 Failed | 每次调用都会返回“BiometricResult.Failed”。无应用程序级锁定。操作系统可能会施加自己的锁定。 | 高的 |

## 9. 模块：密码存储库

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| 回购-001 | 密码库 | 解锁 | 正常：创建和检索条目 | 金库已解锁；有效的数据密钥 | 1. 创建条目（标题=“Test”，用户名=“user”，密码=“pass”） 2. 通过返回的ID检索 | 解密后检索到的条目具有匹配的标题、用户名、密码 | 批判的 |
| 回购-002 | 密码库 | 解锁 | 正常：数据库中所有字段均已加密 | 条目已创建 | 1. 创建条目 2. 直接读取原始数据库行（绕过解密） | `encrypted_title`、`encrypted_username`、`encrypted_pa​​ssword` 包含带有 `version`、`iv`、`ciphertext`、`tag` 键的 JSON。没有一个包含明文。 | 批判的 |
| 回购-003 | 密码库 | 解锁 | Normal：元数据以明文形式存储 | 使用category=“social”创建的条目，isFavourite=true | 1. 创建条目 2. 读取原始数据库行 | 明文形式的`category == "social"`、`is_favorite == 1`、`security_mode == 0`。 `created_at` 和 `updated_at` 是明文整数。 | 中等的 |
| 回购-004 | 密码库 | 解锁 | 正常：更新使用新的随机数重新加密 | 条目存在 | 1. 读取原始行，注释 IV 2. 更新条目 3. 再次读取原始行 | IV 已更改（每个加密都使用新的随机数） | 高的 |
| 回购-005 | 密码库 | 解锁 | 正常：删除删除行 | 存在 ID 已知的条目 | 1. `deleteById(id)` 2. `getById(id, dataKey)` | 返回空值。行不再存在于数据库中。 | 高的 |
| 回购-006 | 密码库 | 解锁 | 正常：getAll 返回所有解密的条目 | 存在 3 个条目 | 1. `getAll(dataKey)` | 返回包含所有明文字段的 3 个 PasswordEntry 对象的列表 | 高的 |
| 回购-007 | 密码库 | 解锁 | 普通：按标题子字符串搜索 | 标题为“GitHub 帐户”的条目已存在 | 1.`搜索（“github”，PasswordFilter（），dataKey）` | 返回包含条目的列表（不区分大小写的匹配） | 高的 |
| 回购-008 | 密码库 | 解锁 | 普通：按用户名搜索 | 用户名=“john@example.com”的条目 | 1.`搜索（“约翰”，PasswordFilter（），dataKey）` | 返回包含条目的列表 | 中等的 |
| 回购-009 | 密码库 | 解锁 | 普通：使用类别过滤器搜索 | “社交”和“工作”类别中的条目 | 1.`搜索（“”，PasswordFilter（类别=“社交”），dataKey）` | 仅返回类别 ==“社交”的条目 | 中等的 |
| 回购-010 | 密码库 | 解锁 | 正常：使用收藏夹过滤器搜索 | 最喜欢和不最喜欢的条目的混合 | 1.`搜索("",PasswordFilter(onlyFavorites=true),dataKey)` | 仅返回具有“isFavorite == true”的条目 | 中等的 |
| 回购-011 | 密码库 | 解锁 | 错误：getById 的 ID 不存在 | 空数据库 | 1. `getById(999, dataKey)` | 返回空值 | 中等的 |
| 回购-012 | 密码库 | 解锁 | 错误：使用空 ID 进行更新 | id=null 的密码条目 | 1. `更新（条目，数据密钥）` | 返回假。没有数据库更改。 | 中等的 |
| 回购-013 | 密码库 | 解锁 | 正常：清除删除所有条目 | 已存在 5 条记录 | 1. `clear()` 2. `getAll(dataKey)` | 返回空列表 | 中等的 |
| 回购-014 | 密码库 | 解锁 | 安全性：securityMode条目使用SecureModeKey | DataKey 和 SecurityModeManager 可用 | 1. 使用 securityMode=true 创建条目 2. 读取原始的 crypto_password 列 3. 尝试直接使用 DataKey 解密 | DataKey解密失败（使用SecureModeKey加密的密码，而不是DataKey） | 批判的 |
| 回购-015 | 密码库 | 解锁 | 正常：getPasswordCipherById 返回原始密码 | securityMode=true 的条目存在 | 1. `getPasswordCipherById(id)` | 返回带有加密字符串且 securityMode=true 的PasswordCipherPayload，无需解密 | 高的 |
| 回购-016 | 密码库 | 解锁 | 正常：可空字段存储为 NULL | url=null、notes=null 的条目 | 1. 创建条目 2. 读取原始行 | `encrypted_url IS NULL`, `encrypted_notes IS NULL` | 低的 |
| 回购-017 | 密码库 | 解锁 | 正常：标签加密为 JSON 数组 | 带标签的条目=[“工作”，“重要”] | 1. 创建条目 2. 检索条目 | `entry.tags == ["工作", "重要"]` | 中等的 |
| 回购-018 | 密码库 | 解锁 | 边缘：使用空查询且无过滤器进行搜索 | 已有 10 条记录 | 1.`搜索（“”，PasswordFilter（），dataKey）` | 返回所有 10 个条目（未应用过滤） | 低的 |

## 10. 模块：截图保护

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| SS-001 | 屏幕安全 | 任何 | 普通（Android）：默认启用屏幕截图保护 | 安卓;应用程序刚刚启动 | 1. 在 SettingsViewModel 初始化后检查窗口标志 | FLAG_SECURE 在窗口上设置 | 高的 |
| SS-002 | 屏幕安全 | 解锁 | 正常（Android）：禁用屏幕截图保护 | 设置：screenshotAllowed = false 最初 | 1. 调用`updateScreenshotAllowed(true)` | FLAG_SECURE 已从窗口中删除。配置保留为“screenshot_allowed=true”。 | 高的 |
| SS-003 | 屏幕安全 | 解锁 | 正常（Android）：重新启用屏幕截图保护 | 屏幕截图允许 = true | 1. 调用`updateScreenshotAllowed(false)` | 在窗口上设置 FLAG_SECURE。配置保留为“screenshot_allowed=false”。 | 高的 |
| SS-004 | 屏幕安全 | 任何 | Edge（桌面）：所有调用均为空操作 | 桌面平台 | 1. 调用 `enableScreenshotProtection()` 2. 调用 `disableScreenshotProtection()` | 没有崩溃。没有效果。 | 低的 |

## 11. 模块：密码生成器

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| GEN-001 | 密码生成器 | 解锁 | 正常：生成强密码 | GeneratorViewModel 可用 | 1. 调用 `generateWithPreset(PasswordPreset.Strong)` | 返回非空密码字符串 | 中等的 |
| GEN-002 | 密码生成器 | 解锁 | 正常：将生成的密码复制到剪贴板 | 密码已生成 | 1. 生成密码 2. 调用 `copyGeneratePassword()` | 剪贴板上的密码。自动清除定时器启动。 | 高的 |
| GEN-003 | 密码生成器 | 解锁 | 安全性：生成的密码以明文形式存储 | 生成密码 | 1. 生成密码 2. 读取原始 ` generated_pa​​sswords` 表 | `password` 列包含明文密码字符串 | 高的 |
| GEN-004 | 密码生成器 | 解锁 | 正常：最近生成的密码限制为 20 个 | 生成25个密码 | 1. 生成25个密码 2. 查询`selectRecentGeneratePasswords` | 准确返回 20 个最近的密码 | 低的 |

---

## 12. 核心流程：Vault 设置（5 个以上测试）

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| 流量设置-001 | 密钥管理器 | 不设置 | 正常：首次创建保管库 | 数据库中没有保管库配置；注册屏幕上的应用程序 | 1. 用户输入主密码“MyStr0ng!Pass” 2. 点击注册 | 状态转换 NOT_SETUP → UNLOCKED。 Vault_config 包含 salt、加密的 DataKey、Argon2 参数。 PlatformKeyStore有DeviceKey。用户导航到 Vault 屏幕。 | 批判的 |
| 流量设置-002 | 密钥管理器 | 不设置 | 正常：DataKey在vault_config中加密 | 设置完成 | 1. 从Vault_config读取“encrypted_data_key_password” 2.解析JSON | 有效的 EncryptedData JSON，版本为“v2”，base64 iv，密文，标签。不是明文。 | 批判的 |
| 流量设置-003 | 密钥管理器 | 不设置 | 正常：PasswordKey 和密码在设置后被擦除 | 设置后拦截内存 | 1. 完成设置 2. 检查原始密码 CharArray 3. 检查 PasswordKey ByteArray | 两者都被擦除（全为零） | 批判的 |
| 流量设置-004 | 密钥管理器 | 不设置 | 错误：当保管库已存在时进行设置 | Vault 配置已存在 | 1. 调用 `setupVault(密码)` | 返回“KeyManagerResult.Error（VaultAlreadySetup）”。没有生成新密钥。没有修改配置。 | 高的 |
| 流量设置-005 | 密钥管理器 | 不设置 | 正常：会话在设置后解锁 | — | 1. 完成设置 2. 检查 `keyManager.state` 3. 检查 `sessionManager.isUnlocked()` | `状态== KeyManagerState.Unlocked`。 `isUnlocked() == true`。 `getDataKey()` 返回有效的密钥。 | 高的 |
| 流量设置-006 | 密钥管理器 | 不设置 | 正常：Argon2 参数保持正确 | — | 1. 完成设置 2. 从Vault_config读取Argon2参数 | `argon2_memory_kb == "131072"`、`argon2_iterations == "3"`、`argon2_parallelism == "4"`、`argon2_output_length == "32"` | 中等的 |
| 流量设置-007 | 密钥管理器 | 不设置 | 错误：设置期间加密失败 | libsodium 未初始化（模拟） | 1. 调用 `setupVault(密码)` | 返回“KeyManagerResult.Error(CryptoError(...))”。密码仍然被清除。没有保留部分配置。 | 高的 |
| 流量设置-008 | 密钥管理器 | 不设置 | 正常：入职→注册→保险库屏幕 | 首次发射；入职未完成 | 1. 完成注册 2. 在注册时输入密码 3. 提交 | 导航：入职 → 注册 → Vault。配置：onboarding_completed=true，vault_setup_completed=true。 | 中等的 |

## 13. 核心流程：Vault 解锁（5+ 测试）

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| 流程解锁-001 | 密钥管理器 | 锁定 | 正常：使用正确密码解锁 | 保险库存在；状态==锁定 | 1. 输入正确的主密码 2. 提交 | 状态转换“锁定”→“解锁”。 DataKey 已加载到会话中。导航至保管库屏幕。密码密钥和密码已擦除。 | 批判的 |
| 流程解锁-002 | 密钥管理器 | 锁定 | 错误：使用错误密码解锁 | 保险库存在 | 1. 输入错误密码 2. 提交 | 返回“KeyManagerResult.Error(InvalidPassword)”。 UI 显示“主密码错误”。状态保持锁定。密码 CharArray 已擦除。 | 批判的 |
| 流程解锁-003 | 密钥管理器 | 锁定 | 安全性：多次尝试错误的密码 | 保险库存在 | 1.快速提交错误10次密码 | 每次尝试都会返回 InvalidPassword。没有锁定。没有延迟。密码每次都被擦除。 Argon2id 计算成本是唯一的限制。 | 批判的 |
| 流程解锁-004 | 密钥管理器 | 锁定 | 正常：生物识别解锁 | 启用生物识别；商店中的设备密钥 | 1. 启动生物识别解锁 2. 生物识别成功提示 | 状态转换“锁定”→“解锁”。从 PlatformKeyStore 检索并加载 DataKey。 | 高的 |
| 流程解锁-005 | 密钥管理器 | 锁定 | 错误：生物识别解锁无需事先密码登录 | PlatformKeyStore 中没有 DeviceKey | 1. 启动生物识别解锁 | 返回“错误（生物识别未注册）”。 UI显示“尚未准备生物识别解锁，请先用主密码登录一次”。 | 高的 |
| 流程解锁-006 | 密钥管理器 | 锁定 | 错误：未设置保管库时解锁 | 没有Vault_config条目 | 1. 调用 `unlockWithPassword(password)` | 返回“错误（VaultNotSetup）”。 UI显示“未检测到已注册保险库，请先注册”。 | 高的 |
| 流程解锁-007 | 密钥管理器 | 锁定 | 正常：PlatformKeyStore 在密码解锁时更新 | 保险库存在 | 1.使用密码解锁 | 调用 `platformKeyStore.storeDeviceKey(dataKey)`，刷新生物识别路径 | 中等的 |
| 流程解锁-008 | 密钥管理器 | 锁定 | 边缘：生物识别设置已禁用，硬件可用 | 存在生物识别硬件；设置=假 | 1. 调用 `unlockWithBiometric()` | UI 显示“请先在设置中开启生物识别”。未显示生物识别提示。 | 中等的 |

## 14. 核心流程：添加密码（5+ 测试）

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| 流量添加-001 | 添加编辑密码 | 解锁 | 正常：添加包含所有必填字段的条目 | 金库已解锁；在保险库屏幕上 | 1. 点击 FAB 2. 输入标题=“GitHub”，用户名=“user@mail.com”，密码=“s3cr3t！” 3. 点击保存 | 条目已创建。所有 3 个字段均在数据库中加密。导航至保管库屏幕。条目出现在列表中。小吃店“已保存”。 | 批判的 |
| 流量添加-002 | 添加编辑密码 | 解锁 | 正常：添加包含所有可选字段的条目 | 保险库已解锁 | 1. 填写必填字段 2. 填写网址、备注、类别、标签、收藏夹 3. 保存 | 所有字段均已加密。 URL 和注释已加密。类别和收藏夹存储为纯文本元数据。 | 高的 |
| 流量添加-003 | 添加编辑密码 | 解锁 | 正常：添加 securityMode=true 的条目 | 金库已解锁；安全模式管理器处于活动状态 | 1. 填写必填字段 2. 打开 securityMode 3. 保存 | 使用 SecureModeKey 加密的密码。其他字段使用DataKey加密。数据库中的“security_mode == 1”。 | 批判的 |
| 流量添加-004 | 添加编辑密码 | 解锁 | 错误：保存按钮被禁用且标题为空白 | 在“添加编辑”屏幕上 | 1. 输入用户名+密码 2. 将标题留空 | 保存按钮被禁用。点击没有效果。 | 中等的 |
| 流量添加-005 | 添加编辑密码 | 解锁 | 错误：保存按钮因用户名空白而被禁用 | 在“添加编辑”屏幕上 | 1. 输入标题 + 密码 2. 将用户名留空 | 保存按钮被禁用 | 中等的 |
| 流量添加-006 | 添加编辑密码 | 解锁 | 错误：保存按钮被禁用且密码为空 | 在“添加编辑”屏幕上 | 1. 输入标题 + 用户名 2. 将密码留空 | 保存按钮被禁用 | 中等的 |
| 流量添加-007 | 添加编辑密码 | 锁定（比赛） | 错误：表单填写和保存之间的会话锁定 | 用户填写表格；保存前会话锁定 | 1. 填写所有字段 2. 会话超时并锁定 3. 点击保存 | `getDataKey()` 返回 null。 UI显示“保险库已锁定”。条目未保存。 | 高的 |
| 流量添加-008 | 添加编辑密码 | 解锁 | Normal：空白时默认类别 | 类别字段留空 | 1. 将类别留空 2. 保存 | 使用类别=“默认”存储的条目 | 低的 |
| 流量添加-009 | 添加编辑密码 | 解锁 | Edge：非常长的密码（10,000 个字符） | — | 1. 输入 10,000 个字符的密码 2. 保存 | 条目保存成功。解密返回精确的 10,000 个字符串。 | 低的 |
| 流量-ADD-010 | 添加编辑密码 | 解锁 | 正常：每个字段使用独立的随机数加密 | 条目已保存 | 1. 保存条目 2. 读取原始数据库行 3. 解析加密标题和加密用户名的 JSON | 两个字段之间的 IV（随机数）值不同 | 高的 |

## 15. 核心流程：找回密码（5+测试）

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| 流-RET-001 | 密码详情 | 解锁 | 普通：查看标准条目 | 条目存在；安全模式=假 | 1. 点击列表中的条目 | 详细信息屏幕显示解密的标题、用户名、URL、注释。密码显示“•••••••••••”。眼睛图标可见。 | 高的 |
| 流-RET-002 | 密码详情 | 解锁 | 正常：切换密码可见性 | 在详细信息屏幕上；标准条目 | 1. 点击眼睛图标 | 显示密码明文。 2. 再次点击眼睛图标 → 再次屏蔽密码。 | 高的 |
| 流-RET-003 | 密码详情 | 解锁 | 普通：复制密码（标准输入） | 在详细信息屏幕上；标准条目 | 1. 点击复制图标 | 剪贴板包含密码。消息：“密码已复制，将在30秒后清除”。自动清除定时器启动。 | 高的 |
| 流-RET-004 | 密码详情 | 解锁 | 正常：复制用户名 | 在详细信息屏幕上 | 1. 点击用户名复制 | 剪贴板包含用户名。提示信息：“用户名已复制，将在30秒后清除”。 | 中等的 |
| 流-RET-005 | 密码详情 | 解锁+SM | 正常：查看安全模式条目 | 条目存在；安全模式=真；全局安全模式开启 | 1. 点击列表中的条目 | 密码显示“•••••••••••”。没有眼睛图标。仅“使用”按钮可见。 | 批判的 |
| 流-RET-006 | 密码详情 | 解锁+SM | 正常：使用密码（安全模式） | 安全模式进入细节 | 1. 点击“使用”按钮 | 通过 usePassword 按需解密密码。复制到剪贴板。解密的字节被擦除。提示：“密码已使用（已复制），将在30秒后清除”。 | 批判的 |
| 流-RET-007 | 密码详情 | 解锁+SM | 安全性：密码在 UI 中永远不可见（安全模式） | 安全模式进入 | 1. 打开条目详细信息 2. 检查所有呈现的文本 | 没有文本可组合项呈现实际的密码字符串。仅呈现“••••••••••••”。 | 批判的 |
| 流-RET-008 | 密码详情 | 锁定（比赛） | 错误：查看时会话锁定 | 详细信息屏幕上的用户；会话超时 | 1. 查看详细信息 2. 会话锁定 3. 点击复制 | `getDataKey()` 返回 null。 UI显示“保险库已锁定”。剪贴板未修改。 | 高的 |
| 流-RET-009 | 密码详情 | 解锁 | 错误：条目被另一个路径删除 | 条目 ID 存在；条目被外部删除 | 1. 导航到详细信息 2. 删除基础行 3. `getById` 返回 null | 没有显示条目。显示错误或空状态。 | 中等的 |
| 流-RET-010 | 密码详情 | 解锁 | 安全性：解密的条目保留在 ViewModel 状态中 | 加载条目，然后导航离开 | 1. 打开条目详细信息 2. 导航回 Vault 屏幕 3. 检查 ViewModel StateFlow | 条目保留在 StateFlow 中，直到收集或重置 ViewModel。没有显式擦除“PasswordEntry”字符串。 | 中等的 |

## 16. 核心流程：删除密码（5+ 测试）

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| 流量-DEL-001 | 密码详情 | 解锁 | 普通：删除标准条目 | 条目存在；安全模式=假；全局安全模式关闭 | 1. 点击“删除” 2. 确认对话框 | 从数据库中删除条目。导航至保管库。列表已刷新，无条目。 | 高的 |
| 流量-DEL-002 | 密码详情 | 解锁 | 正常：取消删除 | 条目存在 | 1. 点击“删除” 2. 在确认对话框中点击“取消” | 条目未删除。用户仍停留在详细信息屏幕上。 | 中等的 |
| 流量-DEL-003 | 密码详情 | 解锁 | 正常：删除 securityMode 条目并进行生物识别重新验证 | securityMode=true 条目；可用生物识别 | 1. 点击“删除” 2. 生物识别提示→成功 3. 确认对话框 | 条目已删除。导航至保管库。 | 批判的 |
| 流量-DEL-004 | 密码详情 | 解锁 | 正常：删除 securityMode 条目并使用密码重新验证 | 安全模式=真；生物识别不可用 | 1. 点击“删除” 2. 密码对话框 → 输入正确的密码 3. 确认对话框 | 条目已删除。导航至保管库。 | 批判的 |
| 流量-DEL-005 | 密码详情 | 解锁 | 错误：删除 securityMode 条目 — 密码错误 | 安全模式=真；生物识别不可用 | 1. 点击“删除” 2. 输入错误的密码 | 消息“主密码错误”。对话框保持打开状态。条目未删除。 | 高的 |
| 流量-DEL-006 | 密码详情 | 解锁 | 错误：删除 securityMode 条目 — 空白密码 | 安全模式=真；生物识别不可用 | 1. 点击“删除” 2. 提交空白密码 | 消息“请输入主密码”。对话框保持打开状态。 | 中等的 |
| 流-DEL-007 | 密码详情 | 解锁 | 错误：删除 securityMode 条目 — 生物识别失败 | 安全模式=真；生物识别返回失败 | 1. 点击“删除” 2. 生物识别→失败 | 消息“生物识别验证失败”。条目未删除。 | 高的 |
| 流量-DEL-008 | 密码详情 | 解锁 | 错误：删除 securityMode 条目 — 生物识别已取消 | 安全模式=真；生物识别退货已取消 | 1. 点击“删除” 2. 用户取消生物识别 | 消息“已取消生物识别验证”。条目未删除。 | 中等的 |
| 流-DEL-009 | 密码详情 | 解锁+SM | Edge：在全局安全模式下隐藏编辑/删除 | 全局安全模式开启；任何条目 | 1. 打开条目详细信息 2. 检查按钮 | “编辑”和“删除”按钮不渲染。无法从 UI 中删除。 | 高的 |
| 流量-DEL-010 | 密码详情 | 解锁 | 边缘：条目 ID 为空 | 状态为 id=null 的 PasswordEntry | 1. 在 ViewModel 上调用 `delete()` | 方法立即返回（无操作）。无数据库操作。没有崩溃。 | 低的 |

## 17. 核心流程：主密码更改（5+ 测试）

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| 流量-PW-001 | 密钥管理器 | 解锁 | 正常：更改主密码 | 金库已解锁；当前密码已知 | 1. 输入当前密码 2. 输入新密码 3. 提交 | 产生新的盐。 DataKey 使用新的PasswordKey 重新加密。更新了Vault_config。会话保持解锁状态。所有条目仍然可以访问。 | 批判的 |
| 流量-PW-002 | 密钥管理器 | 解锁 | 正常：修改密码后DataKey不变 | 保险库已解锁 | 1. getDataKey() → keyBefore 2. 更改密码 3. getDataKey() → keyAfter | keyBefore == keyAfter （DataKey 本身永远不会改变） | 批判的 |
| 流量-PW-003 | 密钥管理器 | 解锁 | 正常：旧密码不再有效 | 密码从A更改为B | 1. 将密码更改为 B 2. 锁定保管库 3. 使用密码 A 解锁 | `错误（无效密码）`。密码A失败。 | 批判的 |
| 福洛-PW-004 | 密钥管理器 | 解锁 | 正常：修改后新密码生效 | 密码从A更改为B | 1. 将密码更改为 B 2. 锁定保管库 3. 使用密码 B 解锁 | 解锁成功。状态→解锁。 | 批判的 |
| 福洛-PW-005 | 密钥管理器 | 解锁 | 错误：当前密码错误 | 保险库已解锁 | 1. 输入错误的当前密码+新密码 2. 提交 | `错误（无效密码）`。 UI 显示“主密码错误”。没有配置更改。会话保持解锁状态。 | 高的 |
| 福洛-PW-006 | 密钥管理器 | 解锁 | 安全性：所有临时密钥均已擦除 | — | 1. 更改密码 2. 检查 currentPasswordKey、newPasswordKey、dataKey 副本、两个密码 | 全部擦除（全部为零） | 批判的 |
| 福洛-PW-007 | 密钥管理器 | 解锁 | 正常：密码更改后条目仍然可以访问 | 已存在 5 条记录 | 1. 更改密码 2. 检索每个条目 | 所有 5 个条目均使用会话 DataKey 正确解密 | 高的 |
| 流量-PW-008 | 密钥管理器 | 解锁 | 安全性：使用 DataKey 更新 PlatformKeyStore | — | 1. 修改密码 | 调用 `platformKeyStore.storeDeviceKey(dataKey)`。生物识别路径已刷新。 | 中等的 |

## 18. 系统状态测试

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| 状态-001 | 导航 | 不设置 | 状态：仅可访问身份验证屏幕 | 全新安装 | 1. 尝试导航至 Vault 屏幕 | 导航保留在 Auth 表面上。无法到达主表面。 | 高的 |
| 状态-002 | 导航 | 不设置 | 状态：入职 → 注册流程 | 全新安装；入职未完成 | 1. 应用程序启动 | 显示入职屏幕。完成后，显示注册屏幕。 | 中等的 |
| 状态-003 | 导航 | 锁定 | 状态：锁定主系统时强制登​​录 | Vault 屏幕上的用户；会话锁 | 1. 会话超时 2. NavGraph LaunchedEffect 触发 | 自动导航到 Auth 表面上的 LoginRoute | 批判的 |
| 状态-004 | 导航 | 锁定 | 状态：保管库操作被阻止 | 状态==锁定 | 1. 尝试从任何 ViewModel 获取“getDataKey()” | 返回空值。操作因“保险库已锁定”而中止 | 批判的 |
| 状态-005 | 导航 | 解锁 | 状态：允许所有保管库操作 | 状态==解锁 | 1. 列出条目 2. 查看详细信息 3. 添加条目 4. 删除条目 5. 生成密码 | 所有操作成功 | 高的 |
| 状态-006 | 导航 | 解锁+SM | 状态：强制实施安全模式限制 | 安全模式开启 | 1. 查看条目 → 密码被屏蔽 2. 尝试查找眼睛图标 → 不存在 3. 检查编辑/删除按钮 → 不存在 | 所有限制同时生效 | 批判的 |
| 状态-007 | 密钥管理器 | 解锁 | 状态：通过手动锁定从 UNLOCKED → LOCKED 转换 | 保险库已解锁 | 1. 调用`lock()` | 状态==锁定。数据键已擦除。导航强制验证。 | 高的 |
| 状态-008 | 密钥管理器 | 解锁 | 状态：通过清除转换 UNLOCKED → NOT_SETUP | 保险库已解锁 | 1. 调用`clear()` | 状态== NOT_SETUP。设备密钥已删除。 Vault_config 无效。 | 高的 |
| 状态-009 | 密钥管理器 | 锁定 | 状态：通过清除转换 LOCKED → NOT_SETUP | 保险库已锁定 | 1. 调用`clear()` | 状态== NOT_SETUP。 | 中等的 |
| 状态-010 | 安全模式 | 解锁 | 状态：SM-1 转换 — 无需授权即可启用 | 安全模式关闭 | 1. 打开安全模式 | 没有显示身份验证提示。坚持为真。 UI 限制生效。 | 高的 |
| 状态-011 | 安全模式 | 解锁+SM | 状态：SM-2 转换 — 禁用需要身份验证 | 安全模式开启 | 1. 将安全模式切换为关闭 2. 出现生物识别提示（或密码对话框） | 必须在切换完成之前进行身份验证。如果取消，SecureMode 将保持开启状态。 | 批判的 |

## 19. 跨平台一致性测试

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| XPLAT-001 | 加密 | 解锁 | 跨平台：加密输出兼容 | Android 和桌面上相同的明文 + 密钥 | 1. 在 Android 上加密 2. 在桌面上解密（反之亦然） | 解密成功。恢复了相同的明文。 | 高的 |
| XPLAT-002 | 生物识别认证 | 任何 | 跨平台：桌面始终返回NotAvailable | 桌面平台 | 1. `isAvailable()` 2. `验证(...)` | `假`, `不可用` | 中等的 |
| XPLAT-003 | 安全剪贴板 | 解锁 | 跨平台：iOS 剪贴板是无操作的 | iOS平台 | 1. `copy("test")` 2. `scheduleAutoClear()` | 没有崩溃。对系统剪贴板没有影响。 | 中等的 |
| XPLAT-004 | 会话管理器 | 解锁 | 跨平台：桌面永远不会自动锁定 | 桌面;没有生命周期钩子 | 1. 运行桌面应用程序 2. 最小化窗口 3. 等待 > 超时 4. 恢复窗口 | 会话保持解锁状态（onAppBackground/onAppForeground 从未被调用） | 高的 |
| XPLAT-005 | 平台密钥库 | 解锁 | 跨平台：Android 使用硬件支持的密钥 | 带有 TEE 的 Android 设备 | 1. `isHardwareBacked()` | 返回真 | 高的 |

## 20. 安全场景测试

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| SEC-001 | 密钥管理器 | 锁定 | 安全性：暴力破解 100 次密码尝试 | 金库已锁；正确的密码是“P@ss123” | 1.提交100个错误密码 2.提交正确密码 | 所有 100 次均因密码无效而失败。第101次成功。没有锁定。每次尝试都会擦除密码。 | 批判的 |
| SEC-002 | 加密 | 解锁 | 安全性：加密字段没有DataKey无法解密 | 进入数据库 | 1. 从原始数据库中提取 crypto_title 2. 尝试使用随机 32 字节密钥进行解密 | 解密失败（身份验证标签不匹配） | 批判的 |
| SEC-003 | 加密 | 解锁 | 安全性：字段之间的密文交换 | 存在两个条目 | 1. 将条目 A 的 crypto_password 与原始 DB 中的条目 B 交换 2. 解密条目 A | 解密成功，但返回条目 B 的密码（使用相同的 DataKey）。各个字段的完整性由 AEAD 标签维护，但如果密钥相同，则跨行交换会成功。 | 高的 |
| SEC-004 | 安全模式 | 解锁+SM | 安全性：usePassword 永远不会向 UI 层公开明文 | 安全模式进入 | 1. 调用 `usePassword()` 2. 跟踪数据流 | 明文的流程是：解密→剪贴板复制→擦除。从未分配给任何 StateFlow 或 UI 状态。 | 批判的 |
| SEC-005 | 会话管理器 | 解锁 | 安全性：锁定后DataKey不可访问 | 会话已解锁 | 1. `lock()` 2. 检查 SensitiveData 包装器 | `isAvailable == false`。底层字节数组被擦除（全为零）。 `getDataKey()` 抛出。 | 批判的 |
| SEC-006 | 密钥管理器 | 解锁 | 安全性：密码更改会擦除所有中间体 | — | 1. 更改密码 2. 检查：currentPasswordKey、newPasswordKey、dataKey 副本、currentPassword、newPassword | 所有 5 个值均被擦除（全为零） | 批判的 |
| SEC-007 | 剪贴板 | 解锁 | 安全性：如果应用程序在 30 秒前被终止，剪贴板不会被清除 | 密码已复制；自动清除预定 | 1. 复制密码 2. 30秒内强制杀死应用程序 3. 读取剪贴板 | 剪贴板仍然包含密码（计时器正在处理中；kill 终止协程） | 高的 |
| SEC-008 | 数据库 | 解锁 | 安全性：明文元数据泄露 | 条目存在各种类别、时间戳 | 1. 无需Dat​​aKey读取原始DB | 攻击者了解：条目数量、类别、收藏夹、使用安全模式、创建/更新时间戳。无法读取标题、用户名、密码、URL、注释、标签。 | 中等的 |
| SEC-009 | 数据库 | 解锁 | 安全性：以明文形式生成密码 | 生成的密码存在 | 1.读取原始` generated_pa​​sswords`表 | 所有生成的密码都以明文形式可读。强度分数和时间戳可见。 | 高的 |
| SEC-010 | 平台密钥库 | 锁定 | 安全性（桌面）：可从首选项恢复 DataKey | 桌面;密钥已存储 | 1. 读取 Java Preferences 文件 2. 提取 XOR 主密钥和加密 blob 3. 对它们进行 XOR | 以明文形式恢复的 DataKey | 批判的 |
| SEC-011 | 加密工具 | 任何 | 安全性：PRNG 用于密钥生成 | — | 1. 检查 `CryptoUtils.generateSecureRandom()` 2. 注意它使用 `kotlin.random.Random.Default` | 随机值是 PRNG 生成的，而不是 CSPRNG。影响所有密钥、随机数和盐。 | 批判的 |
| SEC-012 | 氩气2Kdf | 任何 | 安全性：密码字符串保留在内存中 | 从密码导出密钥 | 1. `deriveKey(password, salt, config)` 2. 触发 GC 3. 扫描堆中的密码字符串 | `password.concatToString()` 在字符串池中创建不可变的字符串。 GC 后字符串可能仍然存在。 | 高的 |
| SEC-013 | 安全模式 | 解锁+SM | 安全性：禁用 SecureMode 拒绝未经身份验证 | 安全模式开启 | 1. 关闭 2. 取消生物识别 3. 关闭密码对话框 | 安全模式保持开启状态。没有配置更改。 | 批判的 |
| SEC-014 | 导航 | 解锁 | 安全性：无论屏幕深度如何，锁定都会强制导航 | 用户深入设置子屏幕 | 1. 导航：Vault → 详细信息 → 编辑（3 个屏幕深） 2. 会话超时 | 导航强制重置为身份验证/登录。所有主堆栈均已清除。 | 高的 |
| SEC-015 | 密钥管理器 | 不设置 | 安全性：保管库擦除使数据无法恢复 | 有 10 个条目的保险库 | 1. 调用 `clear()` 2. 尝试解密任何条目 | DataKey 从内存和 PlatformKeyStore 中消失。如果没有主密码+盐（位于应用程序级别无效的vault_config中），则无法派生DataKey。条目永久无法访问。 | 批判的 |

## 21. 边缘情况测试

| 测试ID | 模块 | 状态 | 设想 | 前提条件 | 步骤 | 预期结果 | 安全等级 |
|---------|--------|-------|----------|---------------|-------|-----------------|----------------|
| 边缘-001 | 会话管理器 | 解锁 | 边缘：恰好在边界处超时 | `lockTimeoutMs = 1000`;经过 = 正好 1000 毫秒 | 1. `onAppBackground()` 2. 等待 1000 毫秒 3. `onAppForeground()` | 条件“elapsed >= lockTimeoutMs”为真。会话锁定。 | 中等的 |
| 边缘-002 | 会话管理器 | 解锁 | Edge：超时 = 0 然后后台 24 小时 | `lockTimeoutMs = 0` | 1. 后台应用 2. 等待 24 小时 3. 前台 | `onAppForeground` 返回 false（超时 <= 0 分支刷新活动时间）。会话保持解锁状态。 | 中等的 |
| 边缘-003 | 密码库 | 解锁 | Edge：并发创建和搜索 | 2个协程同时运行 | 1. 协程 A：创建条目 2. 协程 B：搜索条目 | 没有崩溃。搜索可能会或可能不会包含新条目，具体取决于时间。 | 中等的 |
| 边缘-004 | 安全模式 | 解锁 | Edge：条目 securityMode=true 但全局切换关闭 | 使用 securityMode=true 创建的条目；稍后禁用全局切换 | 1.查看条目详情 | 密码仍使用 SecureModeKey（每条目标志）加密。 UI 行为取决于每个条目标志（密码屏蔽，显示“使用”按钮）。 | 高的 |
| 边缘-005 | 密钥管理器 | 解锁 | 边缘：在活动条目加载期间更改主密码 | 密码更改正在进行中；并发条目负载 | 1. 开始修改密码 2. 同时加载条目 | 条目加载使用更改前获取的 DataKey 副本。 DataKey 是相同的值。两次操作均成功。 | 中等的 |
| 边缘-006 | 加密 | 解锁 | 边缘：加密字段最大数据量为 10 MB | 10 MB 字节数组 | 1. `加密(10MB_array, 密钥)` | 成功了。返回的 EncryptedData 带有 10MB 以上的密文。 | 低的 |
| 边缘-007 | 加密 | 解锁 | 边缘：加密字段超过 10 MB | 11 MB 字节数组 | 1. `加密(11MB_array, 密钥)` | 行为由 libsodium 决定。可能成功也可能失败，具体取决于实施情况。 | 低的 |
| 边缘-008 | 密码库 | 解锁 | Edge：搜索解密所有 500 个条目 | 数据库中有 500 个条目 | 1.`search("不存在",PasswordFilter(),dataKey)` | 所有 500 个条目均已解密到内存中以供客户端搜索。返回空列表。内存短暂包含 500 个明文条目。 | 中等的 |
| 边缘-009 | 设置视图模型 | 解锁 | 边缘：不支持的超时值标准化 | 超时设置为 999999（不支持设置） | 1. `updateSessionTimeout(999999)` | 标准化为默认 300,000（5 分钟）。不存储为 999999。 | 低的 |
| 边缘-010 | 设置视图模型 | 解锁 | Edge：已迁移旧版立即超时 | 配置包含 `session_lock_timeout_ms = "1000"`（旧版） | 1. 加载设置 | 从 1000 标准化为 -1 (IMMEDIATE_BACKGROUND_LOCK_TIMEOUT_MS) | 低的 |
| 边缘-011 | 加密 | 解锁 | Edge：带有额外 JSON 字段的存储格式 | 带有附加未知字段的 JSON 字符串 | 1. `EncryptedData.fromStorageFormat` 以及 JSON 中的额外字段 | 解析成功（ignoreUnknownKeys = true）。忽略额外字段。 | 低的 |
| 边缘-012 | 密码库 | 解锁 | Edge：标签 JSON 解析失败 | 损坏的 crypto_tags 解密为无效 JSON | 1. 解密标签 2. JSON.decode 失败 | 返回空列表（被 `runCatching { ... }.getOrElse {emptyList() }` 捕获） | 低的 |

---

## 可追溯性矩阵

| SRS 要求 | 测试 ID |
|-----------------|----------|
| SO-1（单独字段加密） | REPO-002、REPO-014、FLOW-ADD-010 |
| SO-2（DataKey 不在磁盘上） | 流量设置-002、SEC-005、SES-015 |
| SO-3（密码从未存储） | KDF-006、KDF-007、流量设置-003、流量-PW-006 |
| SO-4（擦拭的短暂材料） | SMM-004、SMM-005、SMM-007、SEC-006、MEM-001、MEM-009 |
| SO-5（剪贴板30秒自动清除） | CLIP-002、CLIP-004、CLIP-005、CLIP-006 |
| SO-6（默认截图保护） | SS-001 |
| SO-7（安全模式密码从不显示） | 流量-RET-005、流量-RET-007、SEC-004 |
| SO-8（会话自动锁定） | SES-006、SES-009、SES-011、流程解锁-001 |
| KM-1（密码从未存储） | KDF-006、KDF-007、流量设置-003 |
| KM-4（密码更改时数据密钥不变） | 流量-PW-002 |
| KM-5（SecureModeKey 按操作擦除） | SMM-004、SMM-005、SMM-007 |
| SEC-CLIP-1（固定 30 秒） | CLIP-002、CLIP-006、SEC-007 |
| SEC-ENC-3（篡改检测） | ENC-010、ENC-011、ENC-012 |
| AC-28（暴力破解） | SEC-001、流程解锁-003 |
| AC-29（数据库盗窃） | SEC-002、SEC-008、SEC-009 |
| AC-31（桌面密钥提取） | PKS-008，SEC-010 |
| KI-1（PRNG 问题） | SEC-011 |
| KI-3（内存中的字符串） | SEC-012 |
| KI-5（检查自动锁定无呼叫者） | SES-010、SES-011 |
| KI-6（桌面无生命周期） | XPLAT-004 |
| KI-7（BiometricState 未连接） | BIO-006、BIO-007、BIO-009 |
